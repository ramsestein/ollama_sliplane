# Dependencias del núcleo: servidor proxy y cliente base.
# El servidor (Docker) instala cryptography vía apt (ver Dockerfile).
# Las dependencias opcionales del cliente (torch, transformers,
# huggingface_hub) están en requirements-client.txt.
cryptography>=42.0
numpy>=1.26
