services:
  ollama:
    build: .
    container_name: llm-propio-ollama
    ports:
      - "8000:8000"                    # proxy cifrado (cara pública)
    environment:
      OLLAMA_MODEL: ${OLLAMA_MODEL:-gemma2:2b}
      OLLAMA_KEEP_ALIVE: "-1"
      ENCRYPTION_SECRET: ${ENCRYPTION_SECRET:?Define ENCRYPTION_SECRET en .env}
      AUTH_USER: ${AUTH_USER:-}
      AUTH_PASSWORD: ${AUTH_PASSWORD:-}
      ALLOWED_IPS: ${ALLOWED_IPS:-}
      PROXY_PORT: "8000"
    volumes:
      - ollama-data:/home/app/.ollama
    # ── Hardening ──────────────────────────────────────────────────────────
    read_only: true                    # sistema de ficheros de solo lectura
    tmpfs:
      - /tmp                           # Ollama necesita /tmp escribible
    cap_drop:
      - ALL                            # sin capacidades del kernel
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

volumes:
  ollama-data:
